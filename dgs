cd ~/Dropbox/diagrams
echo ""
echo "searching filenames..."
echo ""
#              only search the basename, not the dirname
find . | ag -i "/[^/]*$1[^/]*$"
cd ~/inflated_diagrams
echo "node search..."
search_diagrams.js "$@"
